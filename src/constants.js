const path = require('path');

const PARTIAL_DIR = path.join(__dirname, '../templates/partials');
const VIEWS_DIR = path.join(__dirname, '../templates/views');

module.exports = {PARTIAL_DIR, VIEWS_DIR};
